# Server Configuration Settings

# General Configuration
# Replace with app name
app_name=

# Backend Configuration
# Replace with the URL of the backend repository (e.g., https://github.com/your_username/your_backend_repo.git)
backend_repo_url=                     

# Replace with the directory name of the backend project within the repository (e.g., your_backend_project)
backend_dir_name=                     

# Replace with the domain name for your backend server (e.g., backend.example.com)
backend_domain_name=                  

# Replace with the absolute path to deploy your backend project
backend_site_path=                    

# Frontend Configuration
# Replace with the URL of the frontend repository (e.g., https://github.com/your_username/your_frontend_repo.git)
frontend_repo_url=                    

# Replace with the directory name of the frontend project within the repository (e.g., your_frontend_project)
frontend_dir_name=                    

# Replace with the domain name for your frontend server (e.g., frontend.example.com)
frontend_domain_name=                  

# Replace with the absolute path to deploy your frontend project
frontend_site_path=                    

# MySQL Configuration
# Replace with the MySQL username for your project (e.g., root or a custom username)
mysql_username=root                   

# Replace with the MySQL password for the provided username (leave empty if none)
mysql_password=                       

# Replace with the name of the MySQL database for your project
mysql_database=                       

# Replace with the MySQL host (usually "localhost" for local development)
mysql_host=localhost                  

# GitHub Access Token
# Replace with your personal access token from GitHub with appropriate permissions (required for private repositories)
github_access_token=                  

# Additional Notes:
# - Make sure you have Apache and MySQL installed and running on your system.
# - These settings are specific to your local development environment and may differ in production or other environments.
# - Please review and update these values before running the setup scripts.
